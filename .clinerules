# Cline Rules

## BlitzBasic / AmiBlitz

- Wanneer er vragen zijn over BlitzBasic of AmiBlitz, of wanneer er BlitzBasic/AmiBlitz-code
  gegenereerd moet worden, mag je de markdown-bestanden in de map `blitzbasic_manuals/` lezen
  als referentie.
- Begin altijd met `blitzbasic_manuals/Guide_To_Blitz_Basic.md`.
- Raadpleeg indien nodig aanvullende markdown-bestanden in dezelfde map voor extra context.

## BlitzBasic / AmiBlitz codeer-conventies

### Hoofdlettergevoeligheid (CRITICAL!)
BlitzBasic/AmiBlitz is **strikt hoofdlettergevoelig** voor alle gereserveerde keywords:
- **Logische operatoren**: `AND`, `OR`, `NOT` — **géén** `and`, `And`, `or`, `not`
- **Control flow**: `IF`, `THEN`, `ELSE`, `ENDIF`, `FOR`, `TO`, `NEXT`, `WHILE`, `WEND`,
  `REPEAT`, `UNTIL`, `GOTO`, `GOSUB`, `RETURN`, `POP`
- **Type-definities**: `NEWTYPE`, `END NEWTYPE`, `DEFTYPE`
- **Variabele types/suffixes** (`.b`, `.w`, `.l`, `.q`, `.f`, `.s`) en veld-accessor `\`
  zijn hoofdletterongevoelig in de IDE (worden door de editor automatisch geformatteerd),
  maar keywords als `AND`, `OR` etc. **moeten exact in HOOFDLETTERS**.

De BlitzBasic-editor herkent keywords in lowercase NIET — de parser ziet `and` als
een onbekende identifier en geeft een compileerfout.

Gebruik dus altijd:
```blitzbasic
If x > 0 AND y < 10     ; correct
If x > 0 And y < 10     ; FOUT — "And" wordt niet herkend
```

## AmiBlitz3 / Amiga code-generatie

Bij het genereren van AmiBlitz3-broncode (`.ab3`) of andere Amiga-tekstbestanden:

### Regeleindes
- Gebruik uitsluitend LF (`\n`, 0x0A) — AmigaDOS-conventie (Unix-stijl).
- Géén CRLF (`\r\n`) en géén losse CR (`\r`); die geven een zichtbare `^M`
  en kunnen de AmiBlitz3-parser laten struikelen.
- Normaliseer altijd: `.replace(/\r\n/g, '\n').replace(/\r/g, '\n')`.
- Zorg dat `.gitattributes`/editor het `.ab3`-bestand niet terug naar CRLF zet
  (bijv. `*.ab3 -text` of als binary markeren).

### Karakters / encoding
- De Amiga gebruikt ISO 8859-1 (Latin-1), een 8-bits tekenset — GEEN UTF-8.
- Bytes worden geschreven als `charCodeAt(i) & 0xFF`; alles boven U+00FF wordt
  afgekapt en raakt corrupt.
- Vermijd/filter niet-ASCII, met name in gebruikersinvoer (zoals flag-namen die
  in comments belanden). Valkuilen: em-dash `—`, en-dash `–`, slimme quotes
  `“ ” ‘ ’`, ellipsis `…`, non-breaking space, box-drawing tekens.
- Het veiligst: puur ASCII (0x20–0x7E) plus LF. Overweeg een sanitizer die
  niet-ASCII weglaat of transliterateert.
- Spaties voor inspringen (geen tabs). Commentaar met `;` (Blitz/AmiBlitz-stijl).

### .ab3-bestanden
- `.ab3` = AmiBlitz3-bron (opvolger van Blitz Basic 2 `.bb`; AmiBlitz2 = `.ab2`).
- De AmiBlitz3-IDE slaat projecten intern/getokeniseerd op. Gegenereerde `.ab3`
  is platte ASCII-source en moet mogelijk via "Load ASCII"/import geladen worden,
  niet als getokeniseerd project geopend.
- Bestandsnamen (AmigaDOS): kort houden, gereserveerde tekens `:` en `/` vermijden,
  en op oude OFS/FFS-volumes namen > ~30 tekens vermijden.

### XINCLUDE plaatsing (CRITICAL!)
`XINCLUDE` voegt de inhoud van een ander bestand letterlijk in op de plek van de
include-directive. De plaatsing is afhankelijk van wat er in het ingevoegde
bestand staat:

- **Data-labels (`.MapData:`, `.FlagData:`, etc.)** — Deze moeten **na `End`**
  komen, omdat AmiBlitz3 labels na `End` verwacht. Zet `XINCLUDE` voor
  data-bestanden dus **direct na `End`**, niet bovenaan het bestand:
  ```blitzbasic
  AMIGA
  End

  XINCLUDE "mapdata.ab3"
  ```

- **Code (procedures, functies, subroutines)** — Deze moeten **vóór de code
  die ze aanroept** staan. Zet `XINCLUDE` voor code-bestanden dus **bovenaan**
  het bestand, of in ieder geval vóór de eerste aanroep:
  ```blitzbasic
  XINCLUDE "player.ab3"

  ; hoofdcode die player-procedures aanroept
  Gosub InitPlayer
  ```

- **Constanten (`#` defines)** — Deze moeten vóór gebruik gedeclareerd zijn,
  dus een `XINCLUDE` met constanten moet **bovenaan** of vóór de eerste
  referentie staan.

**Vuistregel**: Bepaal altijd wat het ingevoegde bestand bevat (code of data),
en plaats `XINCLUDE` op de positie waar die inhoud in het hoofdprogramma thuishoort.

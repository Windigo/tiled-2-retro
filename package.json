{
  "name": "tiled2retro",
  "version": "1.0.0",
  "description": "Tiled2Retro — Tiled map to AmiBlitz3 converter + IFF Converter",
  "main": "main.js",
  "scripts": {
    "start": "tsc && tsc -p tsconfig.viewer.json && electron .",
    "build": "tsc && tsc -p tsconfig.viewer.json && electron-builder",
    "compile": "tsc && tsc -p tsconfig.viewer.json"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "electron": "^42.5.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}
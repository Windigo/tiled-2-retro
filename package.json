{
  "name": "retro-map-editor",
  "version": "2.0.0",
  "description": "Retro Map Editor for AmiBlitz3 — Tiled Viewer + IFF Converter",
  "main": "main.js",
  "scripts": {
    "start": "tsc && tsc -p tsconfig.viewer.json && electron .",
    "build": "tsc && tsc -p tsconfig.viewer.json && electron-builder",
    "compile": "tsc && tsc -p tsconfig.viewer.json"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.0.0",
    "electron": "^42.5.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  }
}